namespace exemplo1 {
    let n1: number;
    n1 = 10;

    let resultado : number;
    resultado = n1 + n1;
    console.log("A soma entre " + n1 +" com " + n1 + " é igual a: " + resultado);

    resultado = n1 - 4;
    console.log(`A subtração entre ${n1} com 4 é igual a: ${resultado}`);

    resultado = n1 * 2;
    console.log (`O dobro de ${n1} é: ${resultado}!!`);

    resultado = n1 / 2;
    console.log (`A metade de ${n1} é: ${resultado}!!`);
}